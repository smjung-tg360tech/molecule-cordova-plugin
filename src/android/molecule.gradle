repositories{
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'molecule.sdk', ext:'jar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}